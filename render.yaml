services:
  - type: web
    name: wonderbooks-backend
    env: java
    rootDir: backend/backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/project_1_backend-0.0.1-SNAPSHOT.jar
